# InsForge (required)
NEXT_PUBLIC_INSFORGE_URL=
NEXT_PUBLIC_INSFORGE_ANON_KEY=
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Admin panel (server-only)
# Project API key — NOT the anon key. Get via: npx @insforge/cli secrets get API_KEY
INSFORGE_API_KEY=

# EODHD market data (server-only)
EODHD_API_KEY= (server-only — do not use NEXT_PUBLIC_)
# Gmail: use an App Password (Google Account → Security → 2-Step Verification → App passwords)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=nicksofficialindia@gmail.com
SMTP_PASSWORD=
SMTP_FROM=nicksofficialindia@gmail.com
SMTP_FROM_NAME=SwingTradingLog Feedback
